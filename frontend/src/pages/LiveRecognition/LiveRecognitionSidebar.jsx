import React from "react";

const LiveRecognitionSidebar = () => {
  return (
    <div
      className={`w-77 transition-all duration-300 ease-in-out flex-shrink-0 `}
    >
      <nav className="fixed top-0 right-0 h-screen bg-transparent border-l border-[#cecece]  flex flex-col transition-all duration-300 ease-in-out w-77 "></nav>
    </div>
  );
};

export default LiveRecognitionSidebar;
