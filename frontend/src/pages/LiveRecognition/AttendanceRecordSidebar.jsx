import React from "react";

const AttendanceRecordSidebar = () => {
  return (
    <div
      className={`w-72 transition-all duration-300 ease-in-out flex-shrink-0 `}
    >
      <nav className="fixed top-0 right-0 h-screen bg-transparent border-l border-[#cecece]  flex flex-col transition-all duration-300 ease-in-out w-72 "></nav>
    </div>
  );
};

export default AttendanceRecordSidebar;
